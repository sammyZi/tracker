<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration
  
  In production, only HTTPS traffic is allowed.
  Debug builds additionally permit cleartext for localhost (Metro bundler).
-->
<network-security-config>
  <!-- Production: HTTPS only -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>

  <!-- Debug: allow cleartext to localhost for Metro bundler -->
  <debug-overrides>
    <trust-anchors>
      <certificates src="user" />
    </trust-anchors>
  </debug-overrides>

  <!-- Allow cleartext only for local dev server -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">localhost</domain>
    <domain includeSubdomains="true">10.0.2.2</domain>
  </domain-config>
</network-security-config>
